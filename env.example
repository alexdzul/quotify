SECRET_KEY=change-me-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
# Database (PostgreSQL)
DB_NAME=quotify
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=127.0.0.1
DB_PORT=5432
# Optional: Base directory for STATIC_ROOT and MEDIA_ROOT
# If not set, BASE_DIR will be used by default
# Example (absolute path):
# FILES_BASE_DIR=/var/www/quotify